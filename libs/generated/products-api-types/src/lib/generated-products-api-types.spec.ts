import { generatedProductsApiTypes } from './generated-products-api-types';

describe('generatedProductsApiTypes', () => {
  it('should work', () => {
    expect(generatedProductsApiTypes()).toEqual('generated-products-api-types');
  });
});
