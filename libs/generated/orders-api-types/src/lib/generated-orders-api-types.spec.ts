import { generatedOrdersApiTypes } from './generated-orders-api-types';

describe('generatedOrdersApiTypes', () => {
  it('should work', () => {
    expect(generatedOrdersApiTypes()).toEqual('generated-orders-api-types');
  });
});
